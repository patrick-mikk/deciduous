# UofT Course Search Tool - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
selenium>=4.35.0
webdriver-manager>=4.0.2
beautifulsoup4>=4.9.0

# Standard library dependencies (included with Python)
# tkinter - GUI framework (usually included with Python)
# sqlite3 - Database support (included with Python)
# re - Regular expressions (included with Python)
# time - Time utilities (included with Python)
# urllib.parse - URL parsing (included with Python)

# Optional development dependencies
# These are not required for normal operation but useful for development
# pytest>=6.0.0  # For unit testing
# black>=21.0.0   # For code formatting