# Copy to `.env` (which is git-ignored) and fill in real values.
# The app loads `.env` at startup via backend/config.py::load_env().

# Google Gemini (requirement-group parsing). Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-flash-latest
