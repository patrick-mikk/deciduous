Generic bordered, lightly-shadowed surface — the base container other data components (CourseCard) build on. Use directly for simple grouped content.

```jsx
<Card><p>Plain content in a card surface.</p></Card>
```
