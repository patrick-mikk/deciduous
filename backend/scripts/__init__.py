"""Operational entry points (DB provisioning, cache warming) — not imported by
the Flask app itself. Each module here is runnable with `python -m
backend.scripts.<name>`; this `__init__.py` only exists so that works.
"""
