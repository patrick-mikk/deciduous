# Backend runtime + test dependencies for the UofT Degree Planner.
# Target: Python 3.12.13 on cPanel (all pins have manylinux wheels — no compiler needed).
# Data layer only; Flask/SQLAlchemy/auth deps are added in Phase 2.

# --- data layer (runtime) ---
requests==2.32.5
beautifulsoup4==4.12.3
lxml==5.3.1

# --- app layer (runtime): Flask API, MySQL persistence, auth, encryption ---
Flask==3.1.3
SQLAlchemy==2.0.38
PyMySQL==1.1.1
bcrypt==4.2.1
cryptography==48.0.1
pdfplumber==0.11.10
itsdangerous==2.2.0    # signed email tokens (verify/reset links); already a Flask dependency, pinned because backend/api/auth.py imports it directly
webauthn==2.2.0        # passkey (WebAuthn) server-side ceremony verification
pyOpenSSL==26.2.0      # webauthn dependency. Must be pinned against `cryptography` above: 25.0.0 caps at cryptography<45 and 26.1.0 at <48, so both fail to resolve against 48.0.1; 26.2.0 allows <49. 26.3.0+ requires cryptography>=49 and would force that pin up.

# --- tests (dev) ---
pytest==9.0.3

# --- dev TUI prototype (not shipped to cPanel web app) ---
prompt_toolkit==3.0.48
