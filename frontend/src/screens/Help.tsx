import { PageHeader } from "@/ds";

/**
 * Stub screen — routed at "/help" (design/01-information-architecture.md).
 * Guides, import instructions, FAQ.
 *
 * This is a placeholder only: the owning screen agent replaces this file's
 * body. Build against src/api (typed client + mock adapter) and src/ds (the
 * 87-component design system) — see frontend/src/api/index.ts and
 * frontend/src/ds/index.ts for the import surface.
 */
export default function Help() {
  return <PageHeader title="Help" subtitle="Stub screen — not yet implemented." />;
}
