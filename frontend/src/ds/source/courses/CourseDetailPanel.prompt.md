Full course detail — header (code + serif title + credit + campus + breadth) and Overview / Sections / Satisfies tabs, with footer add-to-plan / add-to-timetable actions. Renders inside a Drawer on mobile, a page on desktop. Composes Tabs, Chip, SectionList, PrereqTree, RequirementMappingList.

```jsx
<CourseDetailPanel course={course} selectedSections={sel} onSelectSection={pick} onAddPlan={add} onAddTimetable={addTt} />
```
